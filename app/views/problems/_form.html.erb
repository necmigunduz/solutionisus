<%= form_with(model: problem) do |form| %>
  <% if problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(problem.errors.count, "error") %> prohibited this problem from being saved:</h2>

      <ul>
        <% problem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :problem_title %>
    <%= form.text_field :problem_title %>
  </div>

  <div class="field">
    <%= form.label :problem_explanation %>
    <%= form.text_area :problem_explanation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
